# 订货商模块改造设计（2026-09-18）

> 需求来源：汪总 2026-09-18 口述三条需求。本文档为实施方案，经确认后分三期落地。
> 现状调研结论：当前订货流程为「下单→待上级审核→审核扣云仓→待付款→后台确认收款(线下)→待发货→收货结算」；**无在线支付、无收货地址、无 SKU/规格**；代理库存是订单推导值（已付采购−已供应）；差价按订单 `parentAgentId` 快照在收货后结算。

---

## 需求 1：下单即付款 + 无库存自动上浮（对应现状差距：支付、地址、状态机）

### 1.1 新状态机（eb_stock_order.status）

| 值 | 含义 | 变化 |
|---|---|---|
| 1 | 待付款 | **下单后默认进入**（原来是待审核） |
| 0 | 待上级审核 | **付款后**才进入（原在下单后）；审核开关关闭时付款后直接 2 |
| 10 | 等待匹配上级 | **新增**：付款成功但直接上级无库存，挂起等 `stock_up_search_hours` |
| 2 | 待发货 | 不变 |
| 3 | 待收货 | 不变 |
| 4 | 已完成 | 不变 |
| -1 | 已驳回 | 不变 |
| -2 | 已取消 | **新增**：待付款超时/主动取消 |

### 1.2 下单主流程（createOrder 重写）

```
选商品(库存类型: 实体/虚拟) → 填收货信息 → 校验直接上级库存
  ├─ 有库存   → status=1 待付款 → 唤起支付(微信JSAPI/余额)
  └─ 无库存   → 返回提示"最近上级无库存，预计等待 X 小时自动向上匹配"（X=stock_up_search_hours）
               → 仍生成 status=1 待付款订单 → 唤起支付
支付回调 payStockOrder(orderNo):
  ├─ parentStockOk → status=0 待上级审核（审核开关关 → 2 待发货）→ checkAndUpgrade
  └─ 无库存       → status=10 等待匹配，up_search_time=now，up_search_num=0
```

- 上级库存判定沿用 `parentHasStock`（推导值）。**付款即视为采购**，推导模型（下级已付款订单自动占上级库存）天然实现"付款就扣上级库存"。
- `processUpSearchOrders` 触发点增加：① 新增定时任务 `StockUpSearchTask`（每 10 分钟扫 status=10 且到期的单，兜底懒触发）；② 状态条件从 `status=0` 改为 `status=10`；③ **付款后（status≥0 已付款）匹配成功才改挂上级并改写 parentAgentId**；改挂后上级库存仍不足则继续向上（现逻辑保留），全链无货挂总部（parentAgentId=0）。
- **差价归属**：`calcDiffReward` 已按 `order.parentAgentId` 结算，且结算时机在收货/完成之后——付款后到发货前 parentAgentId 可能被改挂，最终"谁审核发货差价归谁"天然成立，无需改算法，只需确保**等待匹配期间不结算**（status=10 不可能进入结算路径，满足"差价先不分"）。

### 1.3 收货地址

`eb_stock_order` 加列：`real_name`、`phone`、`user_address`（下单时填写；上级/后台发货页展示）。

### 1.4 在线支付接入（关键新工程）

- 新增 `StockPayService`：复用商城支付底座（`WechatPayInfoService`/`WeChatPayService`/余额 `yuePay`）。
- 支付单：`eb_wechat_pay_info` 增加 `order_type` 区分（store_order / stock_order），回调 `CallbackService` 增加 stock 分支 → 调 `payStockOrder`。
- 接口：`POST api/front/stock/order/pay`（入参 orderNo + payType：weixin/yue）→ 返回 jsConfig 或余额扣减结果；余额支付同步成功即 `payStockOrder`。
- 待付款超时取消：复用/参考商城订单取消任务，或定时任务扫描 status=1 超 24h 置 -2。

---

## 需求 2：虚拟库存（对应差距：库存类型、虚拟库存表、提货单）

### 2.1 数据模型

- `eb_stock_order` 加列：`stock_type tinyint`（1=实体 2=虚拟），`order_type tinyint`（1=采购 2=虚拟提货 3=换货）。
- 新表 `eb_stock_virtual_stock`：
  `id / uid / product_id / product_name / image / sku_key(预留) / num / source_order_no / remain_num / create_time / is_del`
  —— 付款成功且 stock_type=2 时：`remain_num += num`（虚拟库存入账）；**实体单不产生虚拟库存**。
- 虚拟单不发货：付款后直接走 status=4 已完成（跳过审核/发货），订单列表对上级与后台正常可见（上级供应记录自动成立）。
  - 若审核开关开启，虚拟单仍需上级审核？→ 设计为**虚拟单付款后直接完成**（无物流语义）；如需审核可后续加开关。

### 2.2 提货（虚拟 → 实物）

- 接口 `POST api/front/stock/virtual/pickup`（虚拟库存 id + 数量 + 收货信息）→ 生成 `order_type=2` 的订单（status=0 待总部处理，parentAgentId 沿用来源订单上级快照，仅记录不参与库存推导）→ 总部后台「发货」扣云仓 → 完成。
- 上级与后台订单列表可见提货单（带 orderType 标识）。

### 2.3 页面

- 会员端订货中心 `index.vue` 宫格新增「虚拟库存」；新页 `virtual.vue`（列表 + 提货按钮 + 换货按钮）。
- `goods.vue` 下单弹层增加「实体/虚拟」选择与收货信息表单。
- 后台 `order/index.vue` 增加 stockType/orderType 列与提货单发货操作。

---

## 需求 3：换货改造（对应差距：规格、换货配置、库存联动、差价支付）

### 3.1 SKU/规格地基（本次必须补）

- 订货价引入规格维度：新表 `eb_stock_price_sku`（`product_id, sku_key, level_id, price`，唯一键三元组）；`eb_stock_price` 保留为商品级默认。
- `eb_stock_order_product` 加列 `sku_key`（空=商品级，兼容存量数据）。
- 库存仍商品级（`eb_store_product.stock`），换货/提货按规格发货但库存扣商品总量（与商城现有 SKU 库存体系解耦，注明取舍）。

### 3.2 换货配置（精确到规格）

- 新表 `eb_stock_exchange_config`：`id / product_id / sku_key / is_exchange(0/1) / allow_targets(JSON: [{productId, skuKey}]) / create_time`。
- 后台「商品与库存」页：每行订货价上方加「是否支持换货」开关 + 「可换商品/规格」配置弹窗（多选）。
- 会员端订货中心：只有 `is_exchange=1` 的（商品,规格）显示「换货」按钮。

### 3.3 换货单改造（eb_stock_exchange 加列）

- 加列：`exchange_type tinyint`（1=订单售后换货[现有流程不动] 2=虚拟库存换 3=实体库存换）、`source_sku_key`、`target_product_id / target_sku_key / target_price / diff_price / diff_pay_status / diff_order_no`。
- 规则：**一次一件**（沿用单商品单数量模型）；可选目标必须在 `allow_targets` 内且 `target_price >= source_price`。
- 流程：
  - **虚拟库存换（type=2）**：选虚拟库存记录 1 件 → 选目标 → 填收货信息 → （差价>0 先支付）→ 总部直接发货（扣云仓）→ `eb_stock_virtual_stock.remain_num-1`（旧品扣减、新品不回虚拟库存）→ 完成。上级与后台均有单据记录。
  - **实体库存换（type=3）**：来源=用户已购实体订单商品（该单已完成）→ 上级审核（总部可介入，复用现有 auditExchangeByHq 的 0→2 通道）→ 上级发新品（扣云仓）→ 库存联动：会员实体库存推导自动变化（旧订单仍是旧品、新品来自新单据）→ 差价>0 先支付，**差价按 `stock_exchange_diff` 比例给上级补差价**（启用 `SOURCE_EXCHANGE`，在换货完成后按比例结算给上级，补齐"高出的部分按差价比例给上级补回应得差价"）。
  - 现有 type=1（订单售后换货）流程与状态机不动，向后兼容。

### 3.4 差价结算

- 换货完成时：`diff = target_price - source_price`（按层级拿货价差，非零售差）。若 `stock_exchange_diff=1`：给实际审核发货的上级补 `diff × 该上级与下级拿货价差比例`（沿用 calcDiffReward 的价差口径，来源标记 SOURCE_EXCHANGE）。

---

## 分期实施计划

| 期 | 内容 | 涉及 |
|---|---|---|
| 一期 | 需求 1：收货地址 + 在线支付接入 + 状态机改造 + 等待匹配定时任务 | eb_stock_order 加列、StockPayService、CallbackService 分支、processUpSearchOrders 改造、StockUpSearchTask、goods.vue 下单改版、order 列表状态、后台订单页 |
| 二期 | 需求 2：虚拟库存 + 提货 | eb_stock_virtual_stock、order_type、virtual.vue、提货接口、后台提货发货 |
| 三期 | 需求 3：SKU 地基 + 换货配置 + 换货流程 + 差价支付 | eb_stock_price_sku、exchange_config、exchange 加列、换货流程改造、差价结算、换货页 |

每期交付：迁移 SQL（幂等，落 crmeb/sql/）+ 后端 + 会员端 + 后台页面 + 冒烟清单。

## 待确认决策点

1. **支付方式范围**：微信 JSAPI + 余额，是否够？（暂按这两个做）
2. **下单是否按规格**：换货必须规格级；建议下单同步支持选规格（一期就把 skuKey 打进明细，取价商品级先行）。确认？
3. **虚拟单是否需要上级审核**：当前设计为付款后直接完成（不审核），可加配置开关。默认：不审核。
4. **待付款超时时长**：默认 24 小时自动取消，可配置。默认即可？
5. **实施节奏**：按一期→二期→三期滚动交付，每期完成冒烟后再进入下一期。确认？

# 本地/服务器开放平台 API 根地址（复制为 one-pass.local.env 后填写，勿提交）
# 须以 / 结尾。启动前： 
#   Windows:  for /f "usebackq delims=" %i in (`type local-dev\one-pass.local.env`) do set %i
#   或直接： set ONE_PASS_API_URL=你的开放平台地址/
# ONE_PASS_API_URL=
